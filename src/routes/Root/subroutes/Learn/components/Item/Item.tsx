import './Item.css';
import React from 'react';

interface ItemProps {
  title: string,
  content: string,
}

export default function Item({ title, content }: ItemProps) {
  return(
    <section>

    </section>
  );
}
